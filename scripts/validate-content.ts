/**
 * Content validation lands in Phase 2 (Zod schemas + referential-integrity
 * checks over recipes/ingredients/lexicon/guides — see docs/03 §8).
 *
 * There is no bundled content yet, so this is an intentional passing stub that
 * keeps the `pnpm validate:content` command available from Phase 0 onward.
 */
console.log(
  "validate:content — no content bundled yet (implemented in Phase 2). ✅",
);
