import { ScreenStub } from "@/components/ui/ScreenStub";

export const metadata = { title: "Einkaufen" };

export default function EinkaufenPage() {
  return (
    <ScreenStub
      title="Einkaufen"
      subtitle="Eine Liste, die mitdenkt."
      blurb="Aus Plan und Rezepten wird automatisch eine sortierte Einkaufsliste — nach Regal gruppiert, ohne Dopplungen."
      phase="Kommt in Phase 5"
    />
  );
}
