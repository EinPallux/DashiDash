import { ScreenStub } from "@/components/ui/ScreenStub";

export const metadata = { title: "Vorrat" };

export default function VorratPage() {
  return (
    <ScreenStub
      title="Vorrat"
      subtitle="Was hast du zu Hause?"
      blurb="Hak deinen Vorrat ab und DashiDash zeigt dir sofort, was machbar ist — inklusive Reste-Retter für alles, was weg muss."
      phase="Kommt in Phase 4"
    />
  );
}
