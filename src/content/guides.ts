import type { Guide } from "@/content/schema";

/**
 * Mini-guides (docs/05 §5). Authored in Phase 6 together with the Lernen tab;
 * recipes will link them via `guideIds` then. Empty for now so imports resolve
 * and `validate:content` can run.
 */
export const guides: Guide[] = [];
